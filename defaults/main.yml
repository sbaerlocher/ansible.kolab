---
# defaults file for sbaerlocher.kolab

kolab:
  password:

domains: []

custom_login:
  - domain:
    logo:
    favicon:

my_role_somerepo_params:
  # Disable GPG checking
  gpgcheck: no
  # Remove the gpgkey option
  gpgkey: null